const grid = document.querySelector('.grid');



createGrid(16);
blackFill();



function createGrid(RowsCols){
    for (let i = 0; i < RowsCols*RowsCols; i++) {
        const grid = document.querySelector('.grid');
    }
}